import { HightlightMeDirective } from './hightlight-me.directive';

describe('HightlightMeDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightMeDirective();
    expect(directive).toBeTruthy();
  });
});
